<resources>
    <!-- Base application theme. -->
    <style name="Theme.ImageBackgroundRemover" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryVariant">@color/primaryLightColor</item>
        <item name="colorOnPrimary">@color/primaryTextColor</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="colorSecondaryVariant">@color/secondaryDarkColor</item>
        <item name="colorOnSecondary">@color/secondaryTextColor</item>
        <!-- Status bar color. -->
        <item name="android:windowBackground">@color/purple_200</item>

        <item name="android:statusBarColor">@color/primaryColor</item>
        <!-- Customize your theme here. -->
    </style>


    <style name="DialogTheme" parent="Theme.MaterialComponents.Dialog.Alert">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="backgroundColor">@android:color/transparent</item>
        <item name="background">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:windowIsFloating">false</item>
    </style>

    <style name="MyAlertDialogTheme" parent="Theme.AppCompat.DayNight.Dialog">
        <item name="colorOnSurface">@color/primaryColor</item>
        <item name="backgroundColor">@color/primaryColor</item>
        <item name="background">@color/primaryColor</item>
        <item name="android:background">@color/primaryColor</item>
        <item name="boxBackgroundColor">@color/primaryColor</item>
        <item name="colorAccent">@color/primaryLightColor</item>
        <item name="android:colorBackground">@color/primaryDarkColor</item>
        <item name="android:windowBackground">@color/primaryDarkColor</item>
        <item name="android:textColor">@color/primaryTextColor</item>
        <item name="editTextBackground">@color/gnt_white</item>
    </style>


</resources>